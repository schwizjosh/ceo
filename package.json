{
  "name": "raysourcelabs-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Raysource Labs Platform - Monorepo containing CEO and Cabinet applications",
  "workspaces": [
    "packages/*",
    "apps/ceo/frontend",
    "apps/ceo/backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "dev:ceo": "npm run dev -w apps/ceo/frontend & npm run dev -w apps/ceo/backend",
    "dev:cabinet": "cd apps/cabinet/backend && python -m uvicorn app.main:app --reload --port 5002",
    "build": "npm run build --workspaces --if-present",
    "build:ceo": "npm run build -w apps/ceo/frontend && npm run build -w apps/ceo/backend",
    "build:shared-auth": "npm run build -w packages/shared-auth",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "clean": "rimraf node_modules apps/*/node_modules packages/*/node_modules apps/*/dist packages/*/dist",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "turbo": "^2.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "author": "Raysource Labs",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/raysourcelabs/platform.git"
  }
}